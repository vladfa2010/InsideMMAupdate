# Release artifacts are stored directly in Git so GitHub Pages can serve them.
# Git LFS is intentionally NOT used for .nupkg/.exe/.zip because GitHub Pages
# serves LFS pointer files instead of the actual binaries.
